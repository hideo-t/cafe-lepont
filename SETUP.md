# カフェるぽん Webサイト — 引き継ぎ書

福島県白河市 カフェるぽん（Café Le Pont）の公式サイト。
静的HTMLのみ。ビルド作業・CMS・ログインは一切ありません。**ファイルを直接編集して、サーバーに上げるだけです。**

---

## 1. ファイル構成

```
cafe-lepont/
├ index.html      … トップ
├ menu.html       … メニュー
├ access.html     … 店舗案内・アクセス
├ sakiori.html    … 裂き織り
├ news.html       … お知らせ
├ SETUP.md        … このファイル（公開時はアップロードしない）
└ assets/
   ├ css/style.css        … 全ページ共通のデザイン。色は先頭にまとめてあります
   └ img/
      ├ _placeholder.svg  … 仮の写真。実写真が全部入ったら削除
      └ route.svg         … 294号からのルート図（★要修正）
```

**ページは5枚から増やさない方針です。** 増えるほど更新が止まったときに古びて見えます。

---

## 2. 公開前に必ずやること（★印の箇所）

ファイル内を **`★`** または **`todo`** で検索すると、未確定箇所がすべて出ます。
ブラウザで見ると**赤い枠**で囲まれて表示されるので、見落としません。

### 2-1. 最優先（これが埋まらないとサイトの目的を果たしません）

| # | 項目 | 場所 | 備考 |
|---|---|---|---|
| 1 | **ランチの最低価格** | `index.html` の「いくらぐらい？」 | `●●●` を実額に。**「高そう」を潰す最重要箇所**。単価1,320円は平均であって下限ではありません |
| 2 | **電話番号** | 全5ページ ×2箇所（フッタ／下部固定バー） | 表示用 `0248-00-0000` と `href="tel:0248000000"` の**両方**を直す |
| 3 | **294号からの道順** | `assets/img/route.svg` | 交差点名・目印・一方通行の進入方向・所要時間・曲がる回数。**このファイル1枚を直せば全ページ反映されます** |
| 4 | **大盛りの条件** | `index.html` / `menu.html` | 無料か、＋いくらか、対象メニューはどれか |

### 2-2. 埋めるべきもの

- メニュー名と全品の価格（`menu.html`）
- ラストオーダーの時刻
- 支払方法（現金のみ／カード可）
- 席数、テーブル・カウンターの内訳（`access.html`）
- お子さま用の椅子・食器の有無
- 裂き織りの価格帯と、作り手の紹介文（`sakiori.html`）

### 2-3. 削除するもの

- [ ] **各ページ冒頭の「制作中」赤バー**（`<div class="wip">` ～ `</div></div>`）— 全5ページ
- [ ] 写真の上に出る黒い帯（`<span class="frame-note">…</span>`）— 実写真に差し替えた分から順に
- [ ] `SETUP.md` はサーバーにアップロードしない
- [ ] `assets/img/_placeholder.svg`（実写真が全部入ったら）

---

## 3. 写真の差し替え方

写真は「あとから差し替えてもレイアウトが崩れない」構造にしてあります。
比率はCSS側で固定しているので、**`src` の値を変えるだけ**です。

```html
<!-- 差し替え前 -->
<img src="assets/img/_placeholder.svg" alt="" width="400" height="300" loading="lazy">
<span class="frame-note">★写真：日替わり定食を寄りで</span>

<!-- 差し替え後（frame-note の行は削除する） -->
<img src="assets/img/lunch-01.jpg" alt="日替わり定食" width="1200" height="900" loading="lazy">
```

**撮影のときのお願い**

- 俯瞰（真上）の物撮りにしない。**斜め上から、寄って**撮る
- 湯気・照り・量が写っているものを選ぶ
- 明るい時間に、窓の光で。フラッシュは使わない
- 横長（4:3）で撮っておくと、そのまま入ります
- 1枚あたり **300KB 以下**に縮小してからアップロードしてください（回線が細いお客さんが多いため）

---

## 4. よくある更新のしかた

### 臨時休業を告知する

`news.html` の `<div class="posts">` の中に、このかたまりを足します。

```html
<article class="post">
  <p class="date">2026-10-05</p>
  <h3>10月10日（金）は臨時休業します</h3>
  <p>都合により1日お休みをいただきます。11日（土）から通常どおり営業します。</p>
</article>
```

**終わったら、そのかたまりごと削除してください。** 古い休業案内が残っていると閉店したように見えます。

### メニューの値段を変える

`menu.html` の該当行の数字だけを直します。

```html
<span class="amt"><span class="n">880</span><span class="yen">円</span></span>
```

`class="todo"` が付いていたら、それも消してください（赤枠が消えます）。

### 道路工事が終わったら

1. `index.html` と `access.html` の **`<div class="alert">` ～ `</div>`** を削除
2. `assets/img/route.svg` の「工事」に関する記述を削除

### 色を変えたくなったら

`assets/css/style.css` の先頭 `:root{ }` の中だけを直します。
そこに書いてある色が、全ページ・全パーツに反映されます。**他の場所に色を直接書かないでください。**

---

## 5. Googleビジネスプロフィールとの整合（★要対応）

**現在、サイトとGoogleビジネスプロフィールの記載が一致していません。**
検索順位に影響するので、公開と同時に必ず揃えてください。

揃える項目は3つです。

| 項目 | サイト側の記載 |
|---|---|
| 名称 | `カフェるぽん` |
| 住所 | `福島県白河市向新蔵50` |
| 営業時間 | 木・金・土・日 `11:00–15:00` ／ 定休 月・火・水 |

> **食べログに夜営業（22時まで）が誤って掲載されています。**
> 夜営業は廃止済みです。食べログ側の修正も申請してください。
> このサイトには夜営業の記述は一切ありません（意図的です。復活させないでください）。

---

## 6. サーバーとドメイン

契約は店舗側で行っていただきます。**静的HTMLだけなので、要件はごく軽いです。**

- PHP・データベース・WordPress は**不要**
- 必要なのは「HTMLファイルを置ける場所」と「独自ドメイン」「常時SSL（https）」の3つだけ
- 容量は写真込みで 100MB もあれば十分

アップロードは、契約したサーバーのファイルマネージャに
`index.html` 以下のファイルとフォルダを**そのままの構造で**置くだけです。

---

## 7. 設計上の決めごと（あとから触る人へ）

意図があってそうしてある箇所です。「直したほうがよさそう」に見えても、理由を確認してから変えてください。

- **夜営業の記述は一切ない。** 廃止済みのため。誤情報の再発を防ぐ意図で、時間の表記は全ページ `11:00 – 15:00` に統一。
- **「カフェ」の語を必ず残している。** 補助金申請時の業態が「カフェで食事もできる店」であるため、「食堂」への完全な言い換えは避けている。
- **お知らせページに「最終更新日」を出していない。** 更新が途切れたことが目立たないようにするため。上段の常設情報だけでページとして成立する構成。
- **裂き織りに在庫表示・売り切れ表示を入れていない。** 一点物のため、正確に運用しようとすると毎日の更新が発生する。「価格帯のみ表示」で運用負荷を0にしている。
- **予約・LINE・SNS・決済へのリンクを置いていない。** 指示どおり。ボタンを増やすほど、電話と地図のクリック率が下がる。
- **手書き風フォントを使っていない。** 日本語Webフォントは1書体で1MB前後あり、回線が細い前提と両立しないため、Noto Sans JP 1書体に絞った（数字のみ Archivo Black／英数字だけなので軽い）。
- **構造化データ（LocalBusiness）に電話番号・URL・座標を書いていない。** 未確定のまま仮の値を入れるとGoogleが誤情報として拾うため。**空欄のままが正解**で、確定してから `index.html` 末尾のJSON-LDに追記する。
- **角丸を使っていない／余白を詰めている。** 「洒落て見えて入りづらい」を避け、大衆食堂側に寄せるための意図的な選択。

---

## 8. 確認用URL（GitHub Pages）について

内容が未確定のうちは、**検索エンジンに載らない確認専用の状態**で公開しています。
店主・関係者にURLを送って、スマホでの見え方を確認していただくためのものです。

検索除外は次の2つで行っています。

- 全5ページの `<head>` にある `<meta name="robots" content="noindex,nofollow">`
  （目印として `REVIEW-ONLY` というコメントが付いています）
- リポジトリ直下の `robots.txt`（`Disallow: /`）

### 本公開に切り替えるとき

順番を間違えないでください。**中身を直してから、検索を許可します。**

1. 第2章の★をすべて埋める（特に**電話番号**。仮の番号のままでは絶対に公開しない）
2. 全5ページの「制作中」赤バー（`<div class="wip">`）を削除
3. 写真を実写真に差し替え、`<span class="frame-note">` を削除
4. 全5ページの `REVIEW-ONLY` の行を削除
5. `robots.txt` を削除、または `Disallow: /` を消す
6. Googleビジネスプロフィールと名称・住所・営業時間を一致させる（第5章）

### `.nojekyll` について

消さないでください。GitHub Pages は既定で `_` から始まるファイルを配信しません。
このファイルがないと `assets/img/_placeholder.svg` が表示されなくなります。
